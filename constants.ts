import * as path from "jsr:@std/path";

export const OUTPUT_BASE = "output";
export const FILTERED_JSON = path.join(OUTPUT_BASE, "filtered.json");
export const FINAL_JSON = path.join(OUTPUT_BASE, "final.json");
export const TABLE_FILE = path.join(OUTPUT_BASE, "table.txt");
